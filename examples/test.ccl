# PEOE
for alpha = 1 to 6:
    for each atom i:
        chi[i] = c[i] * q[i] ^ 2 + b[i] * q[i] + a[i]
    done

    for each atom i:
        q[i] = q[i] + (sum[j]((chi[j] - chi[i]) / d[i]) + sum[k]((chi[k] - chi[i]) / d[k])) * ((1 / 2) ^ alpha)
    done
done

where

a is atom parameter
b is atom parameter
c is atom parameter
j is atom such that bonded(i, j) and chi[j] > chi[i]
k is atom such that bonded(i, k) and chi[k] < chi[i]
d[i] = 20.02 if element(i, "hydrogen")
d[i] = a[i] + b[i] + c[i]
